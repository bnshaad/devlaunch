rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // DevLaunch profile photos are stored in Cloudinary. Firebase Storage is
    // intentionally unused so the Spark plan remains supported.
    match /{path=**} {
      allow read, write: if false;
    }
  }
}
